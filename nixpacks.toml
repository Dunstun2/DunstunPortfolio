# Railway deployment configuration
[phases.setup]
nixPkgs = ["nodejs-18_x"]
nixpkgsArchive = "ba913eda2df8eb72147259189d55932012df6301"

[phases.install]
cmds = ["cd backend && npm install --production=false"]

[start]
cmd = "cd backend && node scripts/init-db.js && npm start"
